# CURRENT STATE — Mogg Mining World

Updated: 2026-08-14
Версия: 0.4.0-stage3 | Minecraft 1.20.1 | Forge 47.2.0 | Java 17

## Current Task
Готовимся к **Этапу 4 — модовые руды и прогрессия глубины**. Ванильные
руды (Этап 3) реализованы, но на сервере/клиенте ещё не проверены вживую.

## What Has Been Completed
- **Этап 1**: каркас проекта, регистрация измерения через датапак.
- **Этап 2**: своя пещерная генерация — биом, noise-settings `caves`
  (без воды/лавы по построению), bedrock-потолок/пол, клиентский рендер
  без неба/облаков.
- **Этап 3**: 8 ванильных руд (coal, copper, iron, gold, redstone, lapis,
  diamond, emerald) своими configured/placed фичами с привязкой к
  stone/deepslate и диапазонами Y под колонну 0..320.
- Багфиксы: `carvers` объект (не массив), `predicate_type` в RuleTest,
  `max_inclusive` в HeightProvider.

## Currently Working On
Этап 3 формально завершён, но **не протестирован в игре** — перед Этапом 4
нужна проверка плотности/высот руд (см. «Как проверить» в
`DEVELOPMENT_STATUS.md`, шаг 3.0).

## Current Problem / Open Questions
1. Плотность руд (count/size/диапазоны) подобрана «на глаз» — реальные
   ощущения в игре неизвестны. Возможна правка placed_feature перед Этапом 4.
2. Пустота между Y=128 и Y=318 (рельеф заканчивается на Y=128) — решается
   на Этапах 4/12.
3. gradle-wrapper.jar не в репозитории — сборка только через GitHub Actions.

## Next Steps
1. Проверить Этап 3 в игре (удалить `world/dimensions/moggminingworld`,
   `/execute in moggminingworld:mining_world run tp @s 0 60 0`, покопать).
2. Подправить count/size/диапазоны руд по результатам.
3. Начать Этап 4: руды `moggite_ore`, `crystal_ore`, `vaporite_ore`,
   прогрессия по глубине, конфиг-заготовка плотностей.

## Do NOT
- НЕ переписывать работающую датапак-генерацию на Java-код.
- НЕ добавлять воду/лаву в генерацию (сознательное решение, см.
  `docs/DECISIONS.md`).
- НЕ менять подходы `block_match`/диапазоны Y без причины.
- НЕ начинать Этап 4 до теста Этапа 3.

## How to Continue (для новой ИИ-модели)
1. Прочитать `AGENTS.md`, этот файл, `docs/ROADMAP.md`, `docs/DECISIONS.md`.
2. Проверить `git status` и последние commits.
3. Изучить `DEVELOPMENT_STATUS.md` (детальный журнал всех шагов и как
   проверять каждый).
4. Не переписывать работающие файлы без необходимости.
5. Продолжить со следующего незавершённого этапа из ROADMAP, не начиная
   реализацию до подтверждения текущего состояния.